# E037 preregistration: independent-seed, task-specific-budget Function comparison

Date: 2026-09-22, before E037 outcomes. E034 baseline-only calibration froze `route_cross_and` at beam 128 / four rounds. E036 baseline-only calibration froze `numeric_symmetric_ge5` at beam 256 / six rounds. Both used tree cost ≤16 and six pilot seeds. These settings produce mid-difficulty **pilot** baselines, not evidence of Function benefit. Use fresh seeds **1520–1525**, excluded from all preceding calibrations, with no reselection after inspecting them. The task-specific beam and round budgets differ; within each task all conditions use its same budget, but any numeric–route contrast remains confounded with that compute difference.

Freeze E026's sole admitted Function without retraining. For every seed and each of the two tasks, run five conditions in this order: `no_transfer` (empty library), `admitted` (unrestricted frozen Function), `one_hop_barrier` (same Function; Function-containing parents may produce children only in round one), `inert_slot` (same Function appears in the initial beam but cannot compose), and `random_matched` (one same-cost, seed/task-specific random Function). This gives **60 searches**. Generate every random control before any search, excluding input, prior/current task, probe/evaluation, admitted, and other random signatures; freeze its expression and cost in the manifest. Do not swap or modify the Function based on E037 results. Use E030 provenance search for all but inert, E028 inert search for inert; discard trace from the persisted records to limit size. Freeze all source hashes and write each record to a progress log immediately.

Primary hypothesis: the one-hop barrier is more helpful relative to unrestricted admission on the numeric task than on the routing task. The paired seed-level exact-rate interaction is `[(barrier − admitted)_numeric − (barrier − admitted)_route]`. Report its mean, unbiased variance, Cohen dz, 20,000-resample bootstrap 95% CI, and two-sided exact sign-flip p. The directional confirmation gate is mean ≥+0.25 **and** exact p≤0.05; otherwise the family-specific effect is not confirmed. Because the two tasks use different compute budgets and only one target per family, even a passed gate is evidence for these frozen settings rather than a general task-category law.

Secondary exact comparisons, Holm corrected as a family of four: numeric barrier−admitted, numeric barrier−inert, route barrier−admitted, and route admitted−random. Also report all five conditions' exact and best-error means/unbiased variances for each task, transfer use in successful expressions, elapsed time, generated signatures, and exact-solution primitive/routing/depth costs. Report the fresh-seed no-transfer success counts even if they become floor or ceiling. Do not extend the seed list or select tasks from these outcomes. A positive one-hop-specific claim additionally requires numeric barrier−inert to be positive and Holm p≤0.05; beating unrestricted admission alone cannot prove it.

Audit 60 unique task/seed/condition records, progress equality, strict JSON, hashes of source and frozen inputs, random controls' cost/signature/uniqueness, and every exact expression on all 64 inputs. Stop after all 60 searches or at 15 minutes of CPU work, preserving an incomplete progress log if needed; do not start another process while this one is running. No external compute, publication, or third-party messages.
