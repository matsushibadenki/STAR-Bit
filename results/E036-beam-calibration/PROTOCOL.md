# E036 preregistration: baseline-only beam-width calibration

Date: 2026-09-22, before E036 search outcomes. E035's predeclared weighted-sum grammar had zero exact successes across six tasks. The `numeric_symmetric_ge5` task reached best error 1/64 on all six E035 seeds, so it is the sole frozen numeric task for this **exploratory difficulty calibration**. The task is selected using baseline error only, not any Function outcome. E034's `route_cross_and@4` remains the frozen routing candidate, and is neither rerun nor reselected here.

Run the E035-frozen `numeric_symmetric_ge5` signature with an empty Function library, E026's signature-stable search, maximum six rounds, and tree cost ≤16. Change only beam width over **64, 128, 192, 256**. Use fresh seeds **1510–1515**, paired across widths, for 24 searches. Width 128 is the E035 configuration on new seeds; 64 is the lower-compute control; 192 and 256 are increasing-width controls. Run every pair regardless of interim results, persist each record immediately, and stop at 24 searches or 15 minutes of compute with incomplete progress saved. Do not add widths or seeds after viewing outcomes.

Primary feasibility: baseline exact 2–4/6 is middle; 0–1/6 is floor; 5–6/6 is ceiling. If multiple widths are middle, freeze the **lowest** width. If none are middle, record the failure and do not run Function comparisons. Any selected numeric width is a pilot choice only; learned-Function efficacy requires new independent seeds, with within-task equal-width controls. Different compute budgets across numeric and routing tasks must be reported when interpreting task-family interactions.

Report exact mean/unbiased variance, best-error mean/unbiased variance, generated unique signatures, elapsed time, and exact-solution primitive/routing/depth cost for each width. Paired secondary contrasts are exact-rate and best-error differences for 128−64, 192−128, and 256−192, seed as unit: mean, unbiased variance, Cohen dz, 20,000-resample bootstrap 95% CI, exact two-sided sign-flip p, and Holm correction separately within the three exact and three error comparisons. Lower error is better; report its signed width difference without changing sign after seeing data.

Audit the E035-frozen truth-table signature, all 24 unique records, progress equality, strict JSON, source hashes, empty library, and every exact expression over all 64 inputs. Because width changes the beam trajectory, do not require prefix equality or per-seed monotonic success/error. Keep the raw failures. This is local CPU single-thread work, with no external compute or publication.
